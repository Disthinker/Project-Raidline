# Home Region 建设镜头控制 v1 ExecPlan

状态：实现与本地自动化完成，等待 exact-head Windows/Ubuntu CI 和用户正常游玩验收。

## 玩家结果与依赖

依赖为用户已验收并以 merge commit `50879bc` 进入主线的 PR #126。开发分支 `codex/home-region-build-camera-v1` 从该精确 `origin/main` 创建。

玩家在基地板块打开 `B` 建设模式后，可用 WASD 平移建设镜头，也可按住鼠标右键拖动画面；滚轮缩放、设施虚像放置和已放置设施右键菜单继续工作。退出建设模式后镜头恢复跟随玩家，角色没有因建设镜头移动而改变世界位置。

## 客户端合同

- 建设镜头只属于 SDL 客户端瞬态，不进入 Profile、HomeRegionLayout、设施事务或存档。
- 打开建设模式时以玩家中心初始化镜头；建设面板与设施放置状态共享同一镜头，购买、移动或取消放置不会无故跳回玩家。
- WASD 在建设面板和放置状态中控制镜头，不控制角色；移动速度按屏幕单位定义，并按当前缩放换算为世界距离，斜向输入归一化。
- 鼠标右键按下后移动至少 4 像素才进入拖拽；拖拽让世界随鼠标移动。低于阈值的右键点击仍用于已放置设施菜单，避免破坏 PR #126 的操作。
- 镜头始终限制在 Home Region 世界边界。改变缩放后立即重新约束，不允许看到地图外空白，也不允许镜头丢失。
- 建设栏区域不启动右键拖拽；开始拖拽或键盘平移时关闭旧设施菜单，选中的设施身份仍可保留。
- 关闭建设模式、打开设施功能页、失去窗口焦点、进入基地危机强制界面、返回主菜单或离开 Base 时销毁镜头瞬态。

## 验证与人工验收

自动化覆盖镜头激活/销毁、WASD 恒定屏幕速度、斜向归一化、不同缩放换算、右键点击/拖拽阈值和四边约束；本地化覆盖新的操作提示。完成 Windows Debug 全目标构建、全量 CTest 与 exact-head Windows/Ubuntu CI 后交给用户正常游玩验收，Codex 不启动游戏。

人工验收：进入基地板块按 `B`，分别以 WASD 和按住右键拖动跨越基地范围；在不同滚轮缩放档位下确认移动方向、速度和边界稳定。短按设施右键应继续打开操作菜单，轻微抖动不应误触拖拽；明显拖动不应误开菜单。放置或移动设施时平移到另一位置并完成绿/红虚像放置，确认角色仍留在原处；关闭建设模式后镜头恢复玩家跟随。

回滚为普通 revert。该切片不改变内容版本、schema、资产位置或领域命令，回滚不会产生存档迁移。

## 实现证据

- 新增 SDL-free `BaseBuildCameraController`，统一处理激活、恒定屏幕速度键盘平移、4 像素右键点击/拖拽仲裁和世界边界约束。
- App 在建设面板与设施放置状态中共享同一镜头；短按右键仍进入设施菜单，拖拽和 WASD 不推进角色移动。
- 新增 5 项建设镜头测试并更新双语操作提示；Windows Debug 全目标构建与 1377/1377 CTest 通过。Codex 未启动游戏。

## 明确排除

不做常规基地游玩自由镜头、边缘滚屏、镜头惯性、旋转镜头、小地图点击跳转、设施远程功能访问、多人输入、正式美术或 Manifest 修改。
