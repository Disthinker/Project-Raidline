# Frontier Exchange 超大主题分区随机地图 v3

## 产品结果与唯一范围

本宏切片把 `Frontier Exchange` 从 2560×1440 的道路样例升级为第一张 25600×14400、可承载完整 Raid 的单一无缝室外地图。地图使用 320×180 个 80 世界单位基础格，并按 16×16 格组织为 20×12 个运行时区块。每局固定包含八个连续主题大区，但其形状、相邻关系和位置随 seed 改变：两个工业厂区、两个物流堆场，以及公路换装区、施工空地、排水绿地/公园、公路服务区各一个。

范围来源为 GDD 的“首张可玩随机大地图”阶段裁决和用户 2026-08-28 批准的 v3 返工计划。PR #110 已以 `7bd3b02` 进入 main；本切片继续使用 `codex/procedural-raid-layout-v1` 和 Draft PR #111，原 2560×1440 验收范围作废。

本切片只使用中英文文字、SDL 几何与色块占位，不绘制旧整图背景，不生成或接入新美术，也不修改美术 Manifest。常规阶段为 20 分钟，归零后进入持续高危，不直接导致 Raid 失败。

## 明确排除

- Macro 2 的完整资源点生态和 Macro 3 的完整遭遇生态；本轮只把既有 Loot 与敌人迁移到合法生成 Socket。
- 程序化室内、多层建筑、第二地图主题、正式 Prefab 编辑器、载具驾驶、实时尸潮和 AI 小队。
- 商场、综合医院、警局及其固定内部模板；它们属于其他地图主题。
- 正式美术、正式 Grab/Scratch/Bite 动画与资源 Manifest 变更。

## 基线、分支与交付边界

- 分支：`codex/procedural-raid-layout-v1`。
- 基线：`origin/main@7bd3b02`。
- Draft PR：#111；自动化、exact-head CI 和用户正常游玩验收前不转为已接受主线事实。
- 版本：content v44、schema v35、layout v3、Raid rules v22；schema v34/layout v2 继续按原快照读取，不在迁移时重新生成。
- Codex 不启动游戏；自动化和 CI 后由用户在至少三个 seed 中正常游玩验收。

## 状态所有权与领域合同

`RaidGeneratedMapLayout` 是 Pending Raid 中的唯一布局事实，并冻结：

- `RaidDistrictSnapshot`：八个连续分区及稳定身份、类型、标签位置和压缩格段；
- `RaidLandmarkPlacementSnapshot`：货运换装中心、制造厂区、公路服务中心三个固定内部模板及随机合法位置；
- `RaidTerrainSpan`：草地、水泥、土地、沥青和仅表现用水洼；
- `RaidOutdoorPropSnapshot`：建筑、车辆、集装箱、设备、路障与非碰撞环境占位；
- `RaidAnchorPlacementSnapshot`：出生、撤离、高危、救援、Loot、敌人、室内入口等玩法锚点；
- 主路、次路、接入路、生成尝试、回退原因和完整布局哈希。

随机流按 `districts / landmarks / roads / terrain / props / anchors` 隔离。道路宽度为主路 4 格、次路 3 格、通路 2 格；先形成连通骨架和至少两个环路，再连接地标 Socket 与全部玩法锚点。生成预算耗尽时使用同一份已验证 layout v3 固定安全骨架，不回退旧背景或 layout v2。

GameplayWorld、碰撞、导航、战术地图和 SDL client 只消费冻结快照。`RaidOutdoorPresentationProjection` 通过 16×16 区块索引仅返回摄像机及一圈缓冲范围；地表、道路、建筑和标签不逐帧遍历全图。权威布局始终完整保存在 Raid 快照中。

导航用静态障碍索引构建稠密可行走格；最近可行走点使用局部环形搜索，远距离路径先经过区块门户图，近距离再进入局部 A*。战术地图使用 80×45 揭图格，只更新玩家附近格并消费预压缩道路、分区与地标投影。

## 内容密度与固定地标

- 18～28 个大型厂房或仓库轮廓；
- 700～1100 个可碰撞建筑、车辆、集装箱或大型设备；
- 1200～1800 个非碰撞小型设备、碎屑和环境占位；
- 140～220 个道路车辆、路障或事故点；
- 60～100 个水洼区域。

三个固定内部结构地标分别为货运换装中心、制造厂区和公路服务中心。货运换装中心绑定现有 Exchange Office 与 Freight Service Bay 固定室内；所有地标通过道路 Socket 接入外部程序路网。

## 实施步骤与退出条件

1. 升级内容、布局和存档合同，完成 layout v3、content v44、schema v35 与 v34/v2 兼容。
2. 生成八个连续分区、三个固定地标、4/3/2 格路网、地表和目标密度环境填充。
3. 把现有出生、撤离、高危、救援、Loot、敌人和室内入口迁移到合法 Socket，并冻结确定性回退。
4. 增加区块化只读表现投影、分层导航和 80×45 战术地图。
5. 完成 Windows Debug 全目标、完整 CTest、提交、推送和 exact-head Windows/Ubuntu CI，最后交由用户正常游玩验收。

## 自动化与人工验收

自动化覆盖至少 128 个固定 seed：八个分区数量与连续性、地标合法分区和道路连接、全部关键锚点可达、空间不非法重叠、同 seed 跨实例哈希一致以及坏种子固定回退。保存测试覆盖 v3 往返、schema v34 快照兼容、非法引用和布局哈希篡改；序列化布局不得超过 4 MiB。

性能回归覆盖约 1000 个静态阻挡和 100 名敌人，Windows Debug 最慢子步保持低于 25 ms；单局生成和验证低于 5 秒；可见静态投影最多查询摄像机附近 12 个区块，成本不随整图面积线性增长。

首轮正常游玩暴露三个验收缺陷并已纳入同一 PR：平均帧率高但主线程存在可见顿挫、环境方块缺少身份、随机分区缺少可读规律。修复合同为：同一可见区块范围复用一份预分配表现投影，世界细节每帧只绘制一次；所有可见 Prop 显示双语类型标签；八区改用 4×2 受控宏观模板、有限边界偏移与镜像变化，工业/物流相邻，主次路沿分区骨架形成环路，大型厂房和仓库按 compound 网格成组放置；80×45 战术地图按已探索分区类型着色。随机差异仍保留在模板朝向、地标、局部支路、玩法锚点和环境细节，不退化为固定地图。

第二轮正常游玩确认移动仍存在规律性顿挫，因此客户端提交节奏继续在本 PR 收束：渲染器优先请求并读取 VSync 实际状态；不支持时按当前显示刷新率（不可用则 60 Hz）使用绝对截止时间和 `SDL_DelayPrecise`，错过截止时间后从当前时刻重新排期，不追赶历史帧。本轮不迁移固定 60 Hz simulation，也不修改移动、碰撞或最大 100 ms 输入时间限制。F9 同时显示 `VSYNC / SOFTWARE FALLBACK`、目标刷新率和 Event/Update/Render-Present/Pacing/Total 的平均、P95 与最大值。

第三轮正常游玩确认上述实现仍会顿挫，说明不能再把驱动报告的 VSync 或 144～240 Hz/远程显示刷新值当作可靠节奏来源。修复把提交上限固定到 60 Hz（低刷新显示保留原生值），并在 VSync 成功时也用同一绝对截止时间只补足剩余预算；已经阻塞到截止时间时不叠加第二个帧周期。玩家 Sprite 与整数 SDL viewport 使用同一像素对齐，六帧移动动画改为能被 60 Hz 整除的每帧 0.10 秒。Windows 文本缓存从“达到 1024 项整库清空”改为单项 LRU 淘汰，阻断动态 HUD/F9 文本造成的周期性纹理重建尖峰。

用户进一步用 F9 观测到 Event/Update/Render-Present/Pacing/Total 的最大值分别约为 `0.22/69.24/5.34/13.72/73.04 ms`：规律性回退发生时全部对象和准星会短暂回到数毫秒前，而 Render/Present 与 Pacing 没有对应峰值。真实 Frontier 冻结地图回归随后稳定复现了 `69.97～71.58 ms` Update 峰值，并确认根因不是提交节奏或导航，而是 Active Raid 每经过一秒真实时间推进一分钟世界时间时，`GameSession` 都复制并完整校验包含数 MiB 冻结布局的 `ProfileState`。当前分支改为由已验证的 Active Raid Profile 原位推进世界时间和幂等时间消费者；正式 Settlement 仍执行完整候选校验和原子保存。相同测试连续十次的最慢 Update 已降为 `0.59～0.76 ms`，并验证世界时间准确推进和最终 Profile 合法。

F10 已扩展为通用运行时开发面板：保留上下/左右调整当前武器参数，`F` 在 `FogOfWar / FullStaticMap` 间切换，`I` 切换无限弹药。完整静态地图只覆盖地表、分区、道路、静态建筑/障碍、地标、三类撤离、高危控制、高级资源区和固定室内入口；它不显示实时敌人、不授予敌情、不改写揭图格、Raid 快照或存档。无限弹药仍验证武器、弹匣、故障和损坏状态，但成功击发只使用可丢弃的候选 Profile，不提交弹药、revision、高水位或其他存档状态。每次成功部署新 Raid 都恢复为启用迷雾并关闭无限弹药。

相对鼠标模式下的实际准星和控制目标现在统一受当前摄像机可见世界矩形加四边 48px 外扩范围约束，允许准星中心越出屏幕约一个当前准星直径，但不能继续移远。抵达外扩边缘会丢弃继续向外的控制/后坐力速度，反向输入可立即找回准星；玩家朝向、准星绘制和权威击发方向继续消费同一个受约束状态，不能出现“UI 受限但子弹仍射向更远屏外”的分叉。地图世界边缘仍是最终硬边界。

后续验收指出代码占位物仍沿用了小地图尺度：轿车 `48×30`、卡车 `68×42`，甚至小于 `64×80` 角色；放置器也只锁住中心格，导致相邻物件在视觉和碰撞矩形上重叠。当前分支把比例升级为冻结生成合同：轿车 `272×108`、卡车 `432×152`、集装箱 `288×112`、道路障碍 `176×34`，工程设备至少 `224×144`；普通厂房/仓库扩展到约 10～18×6～11 个 80 单位格，固定地标内建筑也同步扩大。道路物件根据邻接道路确定横纵朝向，除碎屑外的每个物件都以完整矩形预占基础格并避让玩法锚点、道路外大型建筑和其他已占物件。128 个固定 seed 现同时验证道路物件数量、关键锚点可达和非碎屑物件两两无重叠。

进入 layout v3 超大地图前，SDL client 会先立即提交一张双语加载画面，再同步执行冻结布局生成和 Deploy；画面显示目标地图、区域/道路/地标布置以及路线/撤离点验证状态。固定小地图不额外显示该画面，存档和领域状态也不保存表现状态。

人工验收只通过正常游玩完成：至少三个 seed 中跨越多个分区、进入固定室内、战斗、搜刮并使用普通或特殊撤离，观察路线、地标和环境差异；同时确认加载画面在超大地图生成期间可见，车辆、设备、集装箱和建筑相对角色具有可信比例且不存在相互穿插。没有调试按钮或单步验收页。

## 风险与回滚

- 大世界逐帧扫描：由区块化投影、空间索引、门户图和远处低频更新阻断。
- 随机布局产生坏种子：所有必要锚点在冻结前统一验证，失败使用固定合法 v3 骨架。
- 内容集中或语义混乱：分区先于填充生成，地标和玩法锚点按分区类型与道路 Socket 约束。
- 旧档漂移：schema v34/layout v2 只读取原快照，不用 v3 重新生成。
- 表现资产阻塞：代码几何与文字是可运行基线，美术以后只能替换投影，不能改变布局真值。

回滚按客户端投影、导航/服务、存档、生成器/内容、文档的逆序普通 revert；禁止重写历史、强推或删除分支。

## 进度

- [x] 冻结 v3 产品范围、版本和旧快照兼容合同。
- [x] 实现八区、三地标、道路、地表、环境填充和确定性回退。
- [x] 将全部既有 Raid 锚点迁移到冻结 Socket。
- [x] 实现区块投影、分层导航和 80×45 战术地图。
- [x] 通过 128-seed、v35/v34、布局体积、区块裁剪和 100 敌人性能定向回归。
- [x] 修复无标签环境物和无规律随机分区，并完成第一轮区块缓存/帧节奏加固；规律性 Update 长帧仍单独跟踪，不能视为已修复。
- [x] 建立 VSync 优先、显示刷新率软件回退的稳定提交节奏，并把 Pacing 纳入 F9 分位数观测。
- [x] 将 F10 扩展为运行时开发面板，加入不改变领域事实的完整静态地图迷雾开关及双语表现。
- [x] 用 60 Hz 绝对截止时间兜底 VSync/高刷新与远程显示，消除整库文本缓存重建，并对齐玩家 Sprite 与移动动画节拍。
- [x] 为 F10 增加不修改 Profile 或存档的无限弹药开关及双语表现。
- [x] 将权威实际准星和控制目标限制在当前可见范围外扩 48px 的可找回区域，外扩边缘的外向速度归零且反向输入立即生效。
- [x] 用真实 Frontier 冻结地图锁定并消除 Active Raid 每秒复制/校验完整 Profile 造成的约 70ms Update 长帧；保持 Render/Present 与 Pacing 诊断分栏。
- [x] 按角色尺度放大车辆、设备、集装箱与建筑，以完整占地消除非碎屑物件重叠，并在超大地图同步生成前提交双语加载画面。
- [x] Windows Debug 全目标和当前完整 CTest 1255/1255。
- [ ] Draft PR #111 exact-head Windows/Ubuntu CI。
- [ ] 用户至少三个 seed 的正常游玩验收。
