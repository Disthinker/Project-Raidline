interface:
  display_name: "Raidline Art Pipeline"
  short_description: "Guard paused Raidline art production and publishing"
  default_prompt: "Use $raidline-art-pipeline only after the user explicitly reopens a named Raidline art package."
